import React from 'react';

export default class Category extends React.Component {
    render() { 
        return (
            <section>
                <h1 className="title">
                    Category name
                </h1>
                
            </section>
    )}
}